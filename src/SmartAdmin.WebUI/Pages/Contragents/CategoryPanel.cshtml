@inject Microsoft.Extensions.Localization.IStringLocalizer<RegisterModel>  _localizer
@model RegisterModel
@{
   // SelectList DirectionList = @ViewData["Directions"] as SelectList;
}
<div asp-validation-summary="All" class="alert alert-primary text-dark"></div>
<div class="form-group row">

    <div class="col-md-2 mat my-auto">
        <label class="form-label" for="name">@_localizer["Service"]</label>

    </div>
    <div class="col-md-10">

        <div class="custom-control custom-checkbox">
            <input type="checkbox" asp-for="InputContragent.IsService" class="custom-control-input" >
            <label class="custom-control-label" for="InputContragent_IsService"></label>
        </div>

        <span class="invalid-feedback" asp-validation-for="InputContragent.IsService"></span>
    </div>
</div>

<div class="form-group row">

    <div class="col-md-2 mat my-auto">
        <label class="form-label" for="name">@_localizer["Direction"] <span class="text-danger">*</span></label>

    </div>
    <div class="col-md-10">

        <select type="text" asp-for="InputContragent.DirectionId" asp-items="Model.Directions" class="custom-select" placeholder="@_localizer["Direction"]">
            <option value="">@_localizer["Choose direction"]</option>
        </select>

        <span class="invalid-feedback" asp-validation-for="InputContragent.DirectionId"></span>
    </div>

</div>
<div class="form-group row">
    <div class="col-md-2 mat">
        <label class="form-label" for="name">@_localizer["Categories"] <span class="text-danger">*</span></label>

    </div>
    <div class="col-md-10">
        <div id="categories">
            @*@foreach (var catetory in Model.Categories)
                {
                    <div class="custom-control custom-checkbox my-2">
                        <input type="checkbox" class="custom-control-input" value="@catetory.IsCheck" name="AssignedPermissions" id="@catetory.Id">
                        <label class="custom-control-label" for="@catetory.Id">@catetory.Name</label>
                    </div>
                }*@
        </div>
    </div>
</div>
@*@section Scripts{
    <script>

    $(function () {
        $("#DirectionId").change(function () {
            console.log($(this).val());
            //$.ajax({
            //    type: "Get",
            //    url: "/Home/ShowProduct?type=" + $(this).val(),  //remember change the controller to your owns.
            //    success: function (data) {
            //        $("#productcontainer").html("");
            //        $("#productcontainer").html(data);
            //    },
            //    error: function (response) {
            //        console.log(response.responseText);
            //    }
            //});
        });
    });
    </script>
}*@
