
@model IndexModel
@using CleanArchitecture.Razor.Infrastructure.Constants.Permission
@using CleanArchitecture.Razor.Domain.Enums
@using  CleanArchitecture.Razor.Application.Common.Extensions
@inject Microsoft.Extensions.Localization.IStringLocalizer<IndexModel>  _localizer
@inject Microsoft.AspNetCore.Authorization.IAuthorizationService _authorizationService
@{
    //SelectList DirectionList = @ViewData["Directions"] as SelectList;
    var _canAcceditation = await _authorizationService.AuthorizeAsync(User, null, Permissions.Contragents.Acceditation);
}
<form id="contragent_form" class="needs-validation" novalidate="novalidate" enctype="multipart/form-data">
    <div class="modal-body">

        @*<form id="js-login" asp-route-returnUrl="@ViewData["ReturnUrl"]" method="post" class="needs-validation" novalidate="novalidate" enctype="multipart/form-data">*@
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="Input.Id" />
        
        <ul class="nav nav-tabs nav-justified" role="tablist">
            <li class="nav-item">
                <a class="nav-link active" data-toggle="tab" href="#tab_1" role="tab"><i class="@(Settings.Theme.IconPrefix) fa-user mr-1"></i> @_localizer["Contragent Info"]</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" data-toggle="tab" href="#tab_2" role="tab"><i class="@(Settings.Theme.IconPrefix) fa-info-square mr-1"></i> @_localizer["Category"]</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" data-toggle="tab" href="#tab_3" role="tab"><i class="@(Settings.Theme.IconPrefix) fa-file mr-1"></i> @_localizer["Files"]</a>
            </li>
            <!--For manager-->
            @if (_canAcceditation.Succeeded)
            {
                <li class="nav-item">
                    <a class="nav-link" data-toggle="tab" href="#tab_4" role="tab"><i class="@(Settings.Theme.IconPrefix) fa-check-square mr-1"></i> @_localizer["Accreditation"]</a>
                </li>
            }
        </ul>

        <div class="tab-content border border-top-0 p-3">
            <div class="tab-pane fade show p-3 active" id="tab_1" role="tabpanel">
                <partial name="../Contragents/Index/InfoPanel" />
            </div>
            <div class="tab-pane fade show p-3" id="tab_2" role="tabpanel">
                <partial name="../Contragents/Index/CategoryPanel" />
            </div>
            <div class="tab-pane fade show  p-3" id="tab_3" role="tabpanel">
                <partial name="../Contragents/Index/FilesPanel" />
            </div>
            @if (_canAcceditation.Succeeded)
            {
                <div class="tab-pane fade show  p-3" id="tab_4" role="tabpanel">
                    <partial name="../Contragents/Index/AccreditationPanel" />
                </div>
            }
        </div>

    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">@_localizer["Close"]</button>

        @if (_canAcceditation.Succeeded)
        {
        <button type="button" class="btn btn-danger"  id="btnReject"   data-toggle="modal" data-target="#RejectModal">@_localizer["Reject"]</button>
        
       
        }
        <button type="submit" class="btn btn-primary" id="saveButton">@_localizer["Accept"]</button>

    </div>
</form>
